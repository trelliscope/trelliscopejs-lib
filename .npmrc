; This project's dependency tree has pre-existing peer-dependency conflicts
; (e.g. @testing-library/react@14 wants react ^18 while the app is on react 19).
; Installs (npm install / npm ci, locally and in CI) require legacy peer resolution.
legacy-peer-deps=true
